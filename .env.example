# Environment variables
# Copy to .env and fill in values

SECRET_KEY=change-me-in-production
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Local development (Docker PostgreSQL on port 55432)
DATABASE_URL=postgres://postgres:postgres@localhost:55432/djgramm

# Docker internal (when running web in Docker)
# DATABASE_URL=postgres://postgres:postgres@db:5432/djgramm
